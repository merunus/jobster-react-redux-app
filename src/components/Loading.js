import React from 'react'

const Loading = ({ center }) => {
    return (
        // <div className={center ? "loading loading-centre" : "loading"}></div>
        <div className="loading loading-center"></div>
    )
}

export default Loading